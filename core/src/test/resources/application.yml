spring:
  application:
    name: core

  jpa:
    generate-ddl: false
    show-sql: true
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
  #        format_sql: true
  flyway:
    locations: classpath:db/migration/test
